# Minimal set the lint workflow needs (see .github/workflows/lint.yml).
#
# Deliberately not requirements.txt: that file also pins every dependency of
# Home Assistant's default_config (ffmpeg, bluetooth, numpy, av...) so a local
# `hass` can actually boot. None of it is reachable from this integration, and
# installing it costs minutes of CI for nothing. homeassistant itself stays,
# since pylint and pyright resolve the integration's imports through it.
#
# Pinned to the latest release rather than to the minimum this integration
# supports (2026.7.0, see hacs.json): what these linters need to catch is a
# symbol that has moved or gone in current Home Assistant. It is also the
# version the devcontainer resolves, so CI and a local `scripts/lint` agree.
homeassistant==2026.7.4
pylint==4.0.4
pyright==1.1.411
ruff==0.15.1

# Not on PyPI: installed straight from its GitHub release, exactly as the
# poetry dev group does (see pyproject.toml).
docstring-linter @ https://github.com/bastgau/docstring-linter/releases/download/v0.5.0/docstring_linter-0.5.0-py3-none-any.whl
